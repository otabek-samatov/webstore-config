server:
  # Host runs fall back to the default; Docker passes SERVICE_PORT (mapped from
  # AUTH_SERVICE_PORT in webstore/.env by docker-compose.yml).
  port: ${SERVICE_PORT:8076}
 
service:
  schemaName: auth_schema
