server:
  # Host runs fall back to the default; Docker passes SERVICE_PORT (mapped from
  # INVENTORY_SERVICE_PORT in webstore/.env by docker-compose.yml).
  port: ${SERVICE_PORT:8074}

service:
  schemaName: inventory_schema
