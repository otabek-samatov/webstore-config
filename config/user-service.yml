server:
  # Host runs fall back to the default; Docker passes SERVICE_PORT (mapped from
  # USER_SERVICE_PORT in webstore/.env by docker-compose.yml).
  port: ${SERVICE_PORT:8075}
 
service:
  schemaName: user_schema
